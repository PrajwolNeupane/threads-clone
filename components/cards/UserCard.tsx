
interface Props {
  id: string;
  name: string;
  username: string;
  image: string;
  personType: string;
}

function UserCard({ id, name, username, image, personType }: Props) {
  return <></>;
}

export default UserCard;
